/**
 * App-wide configuration. In a real app, source these from
 * react-native-config / environment-specific files.
 */
export const API_BASE_URL = 'https://api.example.com';
export const API_TIMEOUT_MS = 15000;

export const APP_NAME = 'AeonApps';
